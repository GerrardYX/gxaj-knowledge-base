# gxaj 知识库 - 测试与交付指南

## 📦 构建产物

### Windows 版本
- 文件：`dist/gxaj知识库 Setup 1.0.0.exe` (74MB)
- 架构：x64
- 安装方式：双击运行，按向导完成安装

### macOS 版本
- 文件：
  - `dist/gxaj知识库-1.0.0.dmg` (96MB) - Intel Mac
  - `dist/gxaj知识库-1.0.0-arm64.dmg` (91MB) - Apple Silicon Mac
- 安装方式：双击 DMG，拖动到 Applications 文件夹
- 注意：未签名，首次打开可能需要在"系统偏好设置 > 安全性与隐私"中允许

---

## ✅ 功能测试清单

### 1. 基础功能测试
- [ ] 启动应用，检查登录界面显示正常
- [ ] 使用默认账号登录（admin / admin123 或 user / user123）
- [ ] 检查主界面布局正常，无卡顿

### 2. 知识库管理测试（管理员账号）
- [ ] 点击"知识库"按钮，打开知识库面板
- [ ] 上传测试文档（Word/PDF/TXT）
- [ ] 检查文档解析进度显示正常
- [ ] 检查向量计算进度显示正常（应显示"正在计算文档向量 (x/xx)"）
- [ ] 上传完成后，文档出现在列表中

### 3. 问答功能测试
- [ ] 在输入框输入问题（例如："这个项目的主要内容是什么？"）
- [ ] 检查是否显示"正在检索相关文档..."状态
- [ ] 检查是否返回相关文档片段
- [ ] 检查回答内容是否来自上传的文档

### 4. 性能测试（重点！）
目标硬件：低配 Intel 奔腾处理器 + 4GB 内存

- [ ] 打开活动监视器（Mac）或任务管理器（Windows）
- [ ] 检查应用启动后 CPU 占用是否 < 30%
- [ ] 上传文档时，检查 CPU 占用是否 < 80%
- [ ] 问答过程中，检查 CPU 占用是否 < 80%
- [ ] 检查是否出现界面卡顿或假死
- [ ] 检查内存占用是否稳定在 500MB 以下

### 5. 稳定性测试
- [ ] 连续问答 10 次，检查是否崩溃
- [ ] 上传 3-5 个文档，检查是否稳定
- [ ] 切换主题（浅色/深色），检查是否正常
- [ ] 查看历史对话，检查是否正常加载

---

## 🔧 已知问题与优化建议

### 1. Web Worker 优化（已实施）
- **问题**：向量计算在主线程执行，导致 UI 卡顿
- **优化**：创建 `embeddings.worker.js`，将计算移入后台线程
- **验证**：需在目标硬件上测试 CPU 占用是否下降

### 2. 代码签名（未实施）
- **问题**：macOS 版本未签名，首次打开需要用户手动允许
- **影响**：内部分发可接受，对外发布需要申请 Developer ID 证书
- **建议**：内测阶段可忽略，正式发布前处理

### 3. localStorage 性能（潜在问题）
- **问题**：对话历史和文档数据存储在 localStorage，大量数据可能导致读写缓慢
- **优化建议**：
  - 限制保存的对话数量（当前已限制 50 条）
  - 限制单条对话的消息数量（当前已限制 60 条）
  - 考虑使用 IndexedDB 替代 localStorage（支持更大存储空间）

---

## 📝 测试报告模板

```
测试日期：____年__月__日
测试人员：_________
测试硬件：_________（CPU / 内存 / 操作系统）

### 基础功能
- [ ] 登录正常
- [ ] 知识库管理正常
- [ ] 问答功能正常

### 性能表现
- 启动后 CPU 占用：____%
- 上传文档时 CPU 占用：____%
- 问答过程中 CPU 占用：____%
- 内存占用：____MB
- 是否卡顿：是 / 否

### 问题与建议
1. 
2. 
3. 

### 结论
□ 通过测试，可以交付
□ 需要优化，建议修改：________________________
```

---

## 🚀 下一步工作

1. **性能验证**：在目标硬件（奔腾 + 4GB）上运行测试，确认 CPU 占用是否达标
2. **bug 修复**：根据测试报告修复发现的问题
3. **代码签名**：申请 Developer ID 证书，对 macOS 版本进行签名
4. **OTA 升级**：实现自动更新功能（当前为手动安装）

---

**构建时间**：2026-05-10 22:48
**构建版本**：1.0.0
**Electron 版本**：28.3.3
**Node.js 版本**：24.14.0
