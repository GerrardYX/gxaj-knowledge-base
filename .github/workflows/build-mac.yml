name: Build macOS App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Merge GGUF model parts
        run: cat vendor/models/Qwen3-0.6B-Q4_K_M.gguf.part_* > vendor/models/Qwen3-0.6B-Q4_K_M.gguf

      - name: Install dependencies
        run: npm ci
        env:
          NODE_LLAMA_CPP_SKIP_DOWNLOAD: "true"

      - name: Build macOS app
        run: npm run build:mac

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-app
          path: dist/*.dmg
          retention-days: 7
